name: Release Placeholder

on:
  workflow_dispatch:

jobs:
  release-placeholder:
    name: Release Disabled
    runs-on: ubuntu-latest
    steps:
      - name: Explain current state
        run: |
          echo "Release and npm publish automation are intentionally disabled."
          echo "This repository currently uses CI only for validation checks."
          echo "Publishing can be added later once npm release flow is ready."
