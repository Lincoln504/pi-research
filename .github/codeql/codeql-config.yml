name: "pi-research CodeQL config"

# Analyze source only. dist/ holds generated esbuild bundles that mirror src/,
# so scanning them yields duplicate findings and spurious syntax-error notes on
# transpiled constructs.
paths-ignore:
  - dist
